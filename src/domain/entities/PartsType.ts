import React from "react"

export type TNavList = {
    id: string
    label: string
}

export type TTabs = {
    id: string,
    name: string,
    icon?: React.JSX.Element
}

export type THorizonInfinity = {
    id: number
    icon: React.JSX.Element
}

